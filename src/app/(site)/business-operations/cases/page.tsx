"use client";
import { FormEvent,useMemo,useState } from "react";import Link from "next/link";import { businessServiceLabels,type BusinessService } from "@/lib/business-cases";
export default function BusinessCasesPage(){const[submitted,setSubmitted]=useState(false);const[caseId,setCaseId]=useState("");const services=useMemo(()=>Object.entries(businessServiceLabels) as [BusinessService,string][],[]);function submitCase(e:FormEvent<HTMLFormElement>){e.preventDefault();setCaseId(`FIN-${new Date().getFullYear()}-${Math.random().toString(36).slice(2,8).toUpperCase()}`);setSubmitted(true)}return <main className="mx-auto max-w-5xl px-4 py-12 sm:px-6"><Link href="/business-operations/agent" className="text-sm font-semibold text-brand-600">← Business Operations Agent</Link><h1 className="mt-5 text-4xl font-bold">Start a Business Operations Case</h1><p className="mt-3 max-w-3xl text-slate-600 dark:text-slate-300">Submit a service request and receive a case reference. Production persistence, authentication, uploads and notifications must be connected server-side before real customer records are accepted.</p>{submitted?<section className="mt-8 rounded-3xl border border-emerald-200 bg-emerald-50 p-8 dark:border-emerald-900 dark:bg-emerald-950/20"><p className="text-sm font-bold uppercase text-emerald-700">Case prepared</p><h2 className="mt-2 text-3xl font-bold">{caseId}</h2><p className="mt-3">Initial status: <strong>Submitted</strong>. Save this reference.</p><button onClick={()=>setSubmitted(false)} className="mt-5 rounded-xl bg-brand-600 px-5 py-3 font-semibold text-white">Create another case</button></section>:<form onSubmit={submitCase} className="mt-8 grid gap-5 rounded-3xl border border-slate-200 p-6 dark:border-slate-800 sm:p-8"><div className="grid gap-5 md:grid-cols-2"><label className="font-semibold">Full name<input required name="clientName" className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent px-4 py-3 font-normal dark:border-slate-700" /></label><label className="font-semibold">Email<input required type="email" name="email" className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent px-4 py-3 font-normal dark:border-slate-700" /></label></div><div className="grid gap-5 md:grid-cols-2"><label className="font-semibold">Phone / WhatsApp<input name="phone" className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent px-4 py-3 font-normal dark:border-slate-700" /></label><label className="font-semibold">Business / proposed name<input required name="businessName" className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent px-4 py-3 font-normal dark:border-slate-700" /></label></div><label className="font-semibold">Service<select required name="service" className="mt-2 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 font-normal text-slate-900 dark:border-slate-700 dark:bg-slate-900 dark:text-white">{services.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="font-semibold">Additional instructions<textarea name="notes" rows={5} className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent px-4 py-3 font-normal dark:border-slate-700" /></label><div className="rounded-xl bg-amber-50 p-4 text-sm text-amber-900 dark:bg-amber-950/30 dark:text-amber-200"><strong>Privacy:</strong> Do not enter NIN, BVN, passwords, card details or banking credentials. Sensitive documents require the future secure-upload service.</div><button className="rounded-xl bg-brand-600 px-5 py-3 font-bold text-white">Prepare Case</button></form>}<section className="mt-10"><h2 className="text-2xl font-bold">Case journey</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{["Submitted","Documents Checked","Payment / Processing","Official Provider","Approved","Domain Setup","Bank / Payment Setup","Completed"].map((x,i)=><div key={x} className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900"><span className="font-bold text-brand-600">{i+1}</span><p className="mt-1 font-semibold">{x}</p></div>)}</div></section></main>}
